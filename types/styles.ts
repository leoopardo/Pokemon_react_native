import { ViewProps } from "react-native";

export interface Theme {
  white: White;
  dark: Dark;
}

export interface Dark {
  container: any;
  text: any;
}

export interface White {
  container: any;
  text: any;
}
